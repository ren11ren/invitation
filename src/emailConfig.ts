const EMAIL_CONFIG = {
    // To enable automatic sending with EmailJS, fill these values:
    // - SERVICE_ID: your EmailJS service id (e.g. 'service_xxx')
    // - TEMPLATE_ID: your EmailJS template id (e.g. 'template_xxx')
    // - USER_ID: your EmailJS public/user id (e.g. 'user_xxx' or public key)
    // - TO_EMAIL: fallback destination email used by the template or `mailto:` fallback
    //
    // Use the template body defined in EMAILJS_TEMPLATE.md.
    SERVICE_ID: 'service_lz20f19',
    TEMPLATE_ID: 'template_so9elz2',
    USER_ID: 'NkBIwuMOSUeE08qLR',
    TO_EMAIL: 'rainierganaden1106@gmail.com',
}

export default EMAIL_CONFIG
